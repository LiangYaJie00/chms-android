<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.CHMSAndroid" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/gradient_blue_background</item>
        <item name="android:windowSplashScreenBackground">
            @drawable/gradient_blue_background
        </item>
        <!-- 其他主题属性 -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
    </style>

    <style name="Theme.CHMS" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- 启动页背景色 -->
        <item name="android:windowSplashScreenBackground">@drawable/gradient_blue_background</item>

        <!-- 启动页图标 -->
        <item name="android:windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>

        <!-- 继承基础主题的其他属性 -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
    </style>
</resources>